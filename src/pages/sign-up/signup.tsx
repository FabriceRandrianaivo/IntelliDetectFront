interface headerType {
    theme: boolean;
    // setTheme: (theme: boolean) => void;
}
const Signup = (props: headerType) => {
    return (
        <div className="app-home">
            <div className="content">
                Signup
            </div>
        </div>
    );
};

export default Signup;